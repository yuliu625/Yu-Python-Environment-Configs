name: visualization_env

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.13
  - pip
  - pip:
    - pandas
    - scikit-learn
    - matplotlib
    - seaborn
    - plotly
# utils
    - pathlib
    - notebook


# conda env create --file=./environments/visualization_env.yaml
