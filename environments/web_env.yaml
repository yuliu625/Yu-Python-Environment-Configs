name: web_env

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.13
  - pip
  - pip:
    - -r ../requirements/web_env.txt
#    - -r requirements/another_config_file.txt
#  - run:
#      - ./finish.sh

