name: openocr_env

channels:
  - defaults
  - conda-forge
  - pytorch

dependencies:
  - python=3.12
  - pytorch
  - torchvision
  - torchaudio
#  - pytorch-cuda
  - pip
  - pip:
    - openocr-python  # OpenOCR


# conda env create --file=./specific_projects/openocr/openocr.yaml
