name: langgraph_env

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.13
  - pip
  - pip:
    - langgraph
    - langchain
# utils
    - notebook
    - pandas
    - pathlib
# tools
#    -


# conda env create --file=./specific_projects/langgraph/langgraph_env.yaml
