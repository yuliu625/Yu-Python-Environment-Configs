name: dl_env

channels:
  - defaults
  - conda-forge
#  - pytorch  # if using conda to install pytorch related dependencies
#  - nvidia  # if install CUDA version torch related dependencies

dependencies:
  - python=3.12
  - pip
  - pip:
    - -r ../requirements/dl_env.txt
    - -r ../requirements/sub_requirements/dl_tools.txt
#  - run:
#      - ./finish.sh

