name: llm_env

channels:
#  - nvidia  # if install CUDA version torch related dependencies
  - pytorch  # if using conda to install pytorch related dependencies
  - conda-forge
  - defaults

dependencies:
  - python=3.12
  - pip
  - pip:
    - -r ../requirements/llm_env.txt
#    - -r ../requirements/sub_requirements/some.txt
#  - run:
#      - ./finish.sh

